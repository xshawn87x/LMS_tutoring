-- ============================================================
-- V30: 학원 자가 개설(온보딩)을 위한 tenant 쓰기 권한
-- ============================================================
-- tenant는 RLS 없는 전역 레지스트리다. 지금까지 앱 롤(lms_app)은 SELECT만 가졌으나,
-- 자가 개설(POST /api/onboarding/academy)에서 신규 기관을 INSERT해야 하므로 권한을 추가한다.
-- (플랫폼 요금제 변경의 UPDATE도 함께 명시 — 이미 있으면 무해.)

GRANT INSERT, UPDATE ON tenant TO lms_app;
